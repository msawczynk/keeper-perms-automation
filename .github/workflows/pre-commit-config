# Updated pre‑commit config – fixes missing hook error
#
# * Upgraded `pre‑commit‑hooks` to v4.9.0 (latest as of 2025‑06‑26)
# * Swapped removed `check-utf8` for `check-byte-order-marker`, which catches
#   non‑UTF‑8 encodings via BOM detection without false positives.
#
# Run `pre-commit autoupdate` periodically to keep pinned revisions fresh.
#
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.9.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-added-large-files
      - id: check-byte-order-marker

  - repo: local
    hooks:
      - id: csv-header-lock
        name: csv header lock
        entry: python -m keeper_auto.template --lint
        language: system
        pass_filenames: true
        files: ^perms/.*\.csv$
